#include <stdio.h>

int diff() {
    
}

int findSmallestDifference(int *arr) {
    int diff;
    for(int i = 0; i < 10; i++) {
        for(int j = 0; j < 10; j++) {
            if(arr[i] == arr[j]) continue;
        }
    }
}

int main(int argc, char **argv) {
    
}